services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: xacrebook
    ports:
      - '55432:5432'
    volumes:
      - xacrebook_pg_data:/var/lib/postgresql/data

volumes:
  xacrebook_pg_data:
